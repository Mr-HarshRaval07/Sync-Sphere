# MongoDB connection string for the backend
# Replace <username>, <password>, and <cluster-address> with your Atlas values.
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-address>.mongodb.net/syncsphere?retryWrites=true&w=majority
DB_NAME=syncsphere
